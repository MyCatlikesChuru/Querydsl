spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/querydsl
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true # ??? ???? , ??????? ???
        format_sql: true # ??? ???? , ??? ?? ??
        use_sql_comments: true # JPLQ 쿼리를 볼 수 있는 설정


logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace # Value? ?? ??? ?? ??? ??? ? ??.